import React from 'react';
import DrafBackend from '../../component/DrafBackend';
import GalAdd from '../../wrappers/dashgallery/GalAdd';

const DashGalleryAdd = () => {
  return (<DrafBackend titlepage="เพิ่มรูปภาพ Gallery">
                <GalAdd />
          </DrafBackend>
        )
}

export default DashGalleryAdd